---
name: EXPIRY PRESSURE
trigger: number of chips remaining >= gameweeks_to_expiry + 1 (gameweeks left including this one)
chip_independent: true
order: 4
---
Warn I am now forced into bad timing; sequence the remaining
chips across the weeks left, one chip per gameweek, naming the
gameweek for each. gameweeks_to_expiry of 0 means this is the
last chance for every chip still remaining.
